<h2>Edit workout</h2>

<%= render "form.html", changeset: @changeset,
                        action: workout_path(@conn, :update, @workout) %>

<span><%= link "Back", to: workout_path(@conn, :index) %></span>
